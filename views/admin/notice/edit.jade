extends ../layout

block content
	form(action='/admin/notice/#{notice.id}',method='POST')
		table.table.table-condensed
			+field(notice.owner, 'owner', 'ID do usuário', 'text')
			+field(notice.order, 'order', 'Ordem', 'number')
			tr
				td 
					label(for='text') Texto:
				td
					textarea(name='text')= notice.text
			+field(notice.url, 'url', 'URL do link', 'text')
			
		button(type='submit').btn.btn-primary Enviar
	a(href='/admin/notice/#{notice.id}').btn Cancelar